### Ce fichier sert seulement de notice d'utilisation du fichier config.json

# Attention! Pour insérer le caractère " il faut utiliser le caractère d'échappement \ placé devant comme ceci: \"

{
    # Mot de passe pour accéder à l'administration (Il faut utiliser un mot de passe long pour plus de sécurité.)
    "PasswordAdmin":"password-for-connect-to-backend",

    # Indique si la partie Administration est accessible (passe automatiquement à false au bout de 5 essais de connexion échoué).
    # Les valeur doivent être "true" ou "false" (sans les double quotes).
    # S'il y a une erreur sur l'écriture, la valeur sera égale à false.
    "backendOnline":true,

    # Indique si la partie visible par les visiteurs est en ligne. False affiche un message indiquant que le site est en maintenance.
    # Les valeur doivent être "true" ou "false" (sans les double quotes). S'il y a une erreur sur l'écriture, la valeur sera égale à false.
    "frontendOnline":false,

    # Permet de personaliser le message lorsque le site est hors ligne
    "offlineMessage":"Le site est actuellement en maintenance.",

    # Permet de personaliser le chemin pour se connecter à l'administration (Backend).
    # Exemple: Si le chemin indiqué est path-for-connection, l'adresse pour se connecter sera:
    # domain.ext/backend/path-for-connection
    # Ceci est une sécurité supplémentaire. Si personne d'autre que l'administrateur connait ce chemin, il sera plus difficile de s'y connecter pour un autre utilisateur.
    "backendConnectionPath":"path-for-connection",


    # C'est ici que doivent êtres placé les informations de connexion à la base de données
    "DbInfo":{
        # Adresse du serveur de la base de données
        "ServerName":"localhost",

        # Nom de la base de données
        "DbName":"data_base_name",

        # Nom d'utilisateur
        "Username":"my_username",

        # Mot de passe de l'utilisateur
        "Password":"my_password"
    }
}